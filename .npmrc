registry=https://registry.npmjs.org/

# Skip optional native deps. The only one that matters for this project is
# magika's optional `@tensorflow/tfjs-node` (~150-300 MB native binary), which
# we never use — Magika runs in the renderer via the WebGL backend.
omit=optional
